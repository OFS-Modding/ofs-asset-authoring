# ModContent

Place textures, sprites, meshes, materials, audio and prefabs here. Assign an
AssetBundle name in the Unity Inspector before building.

Do not place mod C# scripts in bundles. Compile code against `OFS.Sdk` in the
normal .NET mod project; Unity bundles should contain data and Unity-native
components only.
